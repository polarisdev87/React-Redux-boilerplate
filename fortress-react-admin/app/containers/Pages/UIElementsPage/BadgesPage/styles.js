const styles = theme => ({
  appbar: {
    margin: theme.spacing(1) * 2,
  },
  margin: {
    margin: theme.spacing(1) * 2,
  },
  padding: {
    padding: `0 ${theme.spacing(1) * 2}px`,
  },
  paper: {
    padding: 20,
  },
});

export default styles;
