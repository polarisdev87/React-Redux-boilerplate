const styles = theme => ({
  formControl: {
    margin: theme.spacing(1) * 2,
    minWidth: 120,
  },
  paper: {
    textAlign: 'center',
    padding: 20,
  },
  root: {
    display: 'flex',
    flexWrap: 'wrap',
  },
  selectEmpty: {
    marginTop: theme.spacing(1) * 2,
  },
  title: {
    fontSize: 14,
  },
});

export default styles;
